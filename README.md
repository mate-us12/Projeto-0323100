# Projeto-0323100

Estrutura de Diretórios:

```
/ProjetoIntroEletrica/
  caminho.csv (arquivo do caminho da ambulância)
  db.sqlite3 (base de dados de usuários e chamados)
   ...
  member1/
    urls.py (lista das funções chamadas por requests HTTP)
    views.py (implementação dessas funções)
    models.py (formato das entradas da base de dados de chamados)
    static/ (CSSs e Imagens)
    templates/ (templates HTML)
  ```
    
